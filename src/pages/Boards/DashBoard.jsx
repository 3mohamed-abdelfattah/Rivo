import React from 'react'
import { Header, Footer } from '@/components';

export const DashBoard = () => {
    return (
        <main>
            <Header />
            <Footer />
        </main>
    )
}
