import React from 'react'
import { Header, Footer } from '@/components';

export const LoginPage = () => {
    return (
        <main>
            <Header />
            <Footer />
        </main>
    )
}
