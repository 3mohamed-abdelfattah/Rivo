import React from 'react'
import { Header, Footer } from '@/components';

export const SignUpPage = () => {
    return (
        <main>
            <Header />
            <Footer />
        </main>
            )
}